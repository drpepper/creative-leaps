{
  "name": "leaps-tablet",
  "version": "1.0.0",
  "description": "Install",
  "main": "bundle.js",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babelify": "^8.0.0",
    "url-search-params-polyfill": "^2.0.2"
  },
  "scripts": {
    "build": "browserify game.js -o bundle.js -t [ babelify --presets [ env ] ]",
    "watch": "browserify game.js -o bundle.js -t [ babelify --presets [ env ] ]",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT"
}
